# PROMPT1: Replace Placeholders in `AGENTS.md`
Analyse this project see wthat is there,
look  for subprojects in  /backend then frontend then tools, you can get some insights from readme files etc...
Then in `AGENTS.md`, replace the placeholders:
- `{PROJECT}` -> Basebox
- `{tbd}` -> Project-specific information - get it from readme 

Return the filled values for `{PROJECT}` and each `{tbd}` you set.

And .. create similar AGENTS.md into each subproject!!!
